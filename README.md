# 多平台電商對帳引擎

把電商賣家每月的手動對帳，從逐筆核對壓縮成一次上傳。
核心是可插拔的帳單解析層與三階段匹配演算法。

> **目前狀態：v0.4 — 開發中。**
> 領域層、解析層與對帳引擎已完成並通過測試；
> API 與前端依序開發中，進度見下方路線圖。
> v1 的 CLI + Excel 版本保留在 [`legacy/`](legacy/)，作為演進的紀錄。

---

## 問題

電商賣家在多個平台銷售，每個平台每月寄來一份結算單。賣家需要確認：
平台撥的錢，跟自己系統裡的訂單對得起來嗎？

實務上這件事很難自動化，因為兩邊的資料對不上的方式有很多種：

- 訂單編號格式不同（平台在結算單上加了前綴、大小寫不一致、混入全形字元）
- 金額有差（費率調整、運費補貼、部分退款）
- 有些結算列根本沒有訂單編號（平台端的調整與沖銷）
- 跨月結算讓日期錯開
- 每個平台的檔案格式、編碼、表頭結構都不一樣

所以這不是一句 SQL JOIN 能解決的問題。

## 做法

```
自有訂單 ─┐
          ├─→ 正規化 ─→ Stage 1 精確匹配 ─→ Stage 2 容差匹配 ─→ Stage 3 模糊匹配 ─→ 四象限報告
平台結算單 ┘                (編號＋金額全等)    (編號同、金額異)     (候選集評分)
```

| 階段 | 條件 | 處理 |
|------|------|------|
| Stage 1 精確 | 正規化後編號與金額皆相符 | 自動勾稽 |
| Stage 2 容差 | 編號相符、金額有差 | 計算差額並歸因（費率／運費／退款／捨入） |
| Stage 3 模糊 | 編號缺失或異常 | 以日期窗口＋金額分桶建候選集，加權評分後進人工確認佇列 |

輸出分為四個象限：**已勾稽**、**金額差異**、**漏記單**（平台有我方無）、
**未撥款**（我方有平台無）。

Stage 3 的關鍵在於先縮小候選集再評分：用日期窗口與金額分桶建索引，
讓每筆未匹配記錄只跟少數候選比對，複雜度從 O(n²) 降到近似 O(n·k)。

## 架構

```
src/reconciliation/
├── domain/          純 Python，零框架依賴。Money、領域模型、匹配演算法
├── parsers/         BaseSettlementParser ＋ 各平台實作 ＋ 註冊表
├── repositories/    介面 ＋ SQLAlchemy 實作
├── services/        使用案例編排，交易邊界
└── api/             FastAPI routers，只處理 HTTP
```

只有一條硬規則：**`domain/` 不 import 任何框架**。
沒有 FastAPI、沒有 SQLAlchemy、沒有 pandas、沒有 openpyxl。
因此領域層的測試不需要資料庫，跑完一輪不到 0.2 秒。

`repositories/` 這一層存在的唯一理由，是讓 SQLite 換成 PostgreSQL
只需要改連線字串——因為沒有任何一行原生 SQL。

## 幾個刻意的設計決策

**金額不用 `float`。** `Money` 值物件以最小貨幣單位的整數儲存，
捨入策略明確指定為 ROUND_HALF_UP。這是從 v1 的 float 實作改過來的，
理由與代價寫在 [ADR 0001](docs/adr/0001-money-as-integer-minor-units.md)。

**非法狀態不可表示。** `MatchResult.__post_init__` 讓「MATCHED 卻沒有 order」
這種狀態根本無法被建構出來。與其在下游到處寫防禦性檢查，不如在型別層擋掉。

**正規化只用於比對，不改寫原始資料。** `Order.external_order_id` 保存平台
實際印出來的字串，正規化只發生在匹配當下——人工查核時必須看得到原文。

**缺失的訂單編號回傳 `None`，不是空字串。** 如果都正規化成 `""`，
缺編號的記錄會在 Stage 1 互相匹配上，產生大量假的對帳結果。

**一筆訂單可以對應多列結算。** `MatchResult.records` 是複數的，
因為同一個訂單編號常常出現一列銷售加一列退款。假設一對一的模型，
只能靠上層硬湊——那正是真實對帳系統最容易出錯的地方。

**無法解釋的差異一律標記為 UNKNOWN。** 把它硬塞進某個看起來合理的分類，
會讓使用者以為系統理解了這筆差異，於是不去追查——那比不歸因更危險。

**分數不夠就不猜。** Stage 3 低於門檻時不會硬配，而是列為漏記單並附上
候選清單與可讀的理由（「金額相差 80 元、訂單後 16 天結算、商品名稱相同」）。
只給一個 0.87 的分數，沒有人敢按下確認。

**測試不只驗案例，還驗性質。** `tests/domain/test_properties.py` 用 Hypothesis
產生隨機輸入，驗證四條無論如何都必須成立的不變量：資料守恆、不重複認領、
金額一致、狀態合法。挑案例只能證明想到的情況是對的。

## 成果

以 300 筆訂單、305 列結算記錄（去重後）的合成資料實測：

| 指標 | 數值 |
|---|---|
| 自動化率（Stage 1，可直接入帳） | **79.6%** |
| 需人工確認 | 20.4% |
| 處理耗時 | 6.2 ms（約 49,000 列／秒） |
| Stage 3 候選比對次數 | 62 次（不建索引需 6,000 次，**97 倍**差距） |

四象限：已勾稽 263、金額差異 25、漏記單 9、未撥款 12。

**自動化率只計 Stage 1**，因為那是唯一可以直接入帳、不需要人看的情況。
把 Stage 2（金額有差）與 Stage 3（模糊比對）算進去數字會跳到 97%，
但那兩種都必須有人確認過——那個數字是自我欺騙。

這些數字不是自己說了算。`scripts/benchmark.py` 會拿資料產生器寫下的
**ground truth** 逐項對答案：

```
✓ 正規化：編號被寫成小寫的訂單仍靠編號命中        9 / 9   （100.0%）
✓ Stage 3 召回率：訂單編號整欄空白的，靠模糊匹配救回  19 / 20  （95.0%）
✓ 分類：平台未撥款的訂單被歸到「未撥款」象限       11 / 11  （100.0%）
✓ 分類：我方查無的結算列被歸到「漏記單」象限        8 / 8   （100.0%）
✓ 分類：注入的金額差異被偵測到                  18 / 19  （94.7%）
✓ 去重：平台重複匯出的列被擋下                 擋下 4 列
✓ 守恆：沒有任何一筆訂單或結算列憑空消失         300/300、305/305
```

### 複雜度

`python scripts/benchmark.py --scaling` 的實測：

| n（訂單數） | 索引版比對次數 | 暴力版比對次數 | 倍數 |
|---:|---:|---:|---:|
| 250 | 78 | 4,750 | 61× |
| 500 | 219 | 18,000 | 82× |
| 1,000 | 914 | 72,000 | 79× |
| 2,000 | 2,315 | 210,000 | 91× |

暴力版是標準的 O(n²)：n 變 8 倍，比對次數變約 44 倍。
索引版明顯低於平方成長，但**不是**線性——n 變 8 倍時約 30 倍，大約 O(n^1.6)。
原因是訂單變多時每個桶裡的候選也跟著變多，所以 O(n·k) 裡的 k 本身會隨 n 成長。
要壓回接近線性，桶寬必須隨資料量縮小。這是目前設計的已知限制，
寫在這裡而不是假裝它是線性的。

## 跑起來看看

目前還沒有 CLI 與 API（W4、W5 的工作），但整條處理鏈已經可以實際運作：

```bash
pip install -e ".[dev]"

python scripts/gen_fixtures.py     # 產生三個平台的合成結算單
python scripts/parse_demo.py       # 讓 registry 自己認平台並解析
python scripts/reconcile_demo.py   # 三階段對帳，輸出四象限報告
python scripts/benchmark.py        # 拿 ground truth 驗證上面的數字
python scripts/walkthrough.py      # 領域層導覽，解釋每個元件在做什麼
```

完整的資料流是：合成資料 → 三個 parser → 統一的領域模型 → 三階段匹配 → 四象限報告。

## 開發

```bash
pytest                   # 或 make test
mypy src                 # 或 make type
ruff check .             # 或 make lint
make check               # 三者一起跑，提交前用這個
```

需求：Python 3.11 以上。Windows 上若 `pytest` 指令被安全性原則擋下，
改用 `python -m pytest`（走簽章過的 python.exe，不經過 pip 產生的啟動器）。

## 路線圖

- [x] **W1 地基** — 分層結構、`Money` 值物件、領域模型、正規化、測試骨架
- [x] **W2 解析層** — 抽象 parser、三個平台實作、合成資料產生器
- [x] **W3 對帳引擎** — 三階段匹配、差異歸因、四象限報告、property-based test
- [ ] **W4 API** — FastAPI、檔案上傳、冪等匯入、OpenAPI
- [ ] **W5 前端** — React ＋ TypeScript，型別由 OpenAPI schema 產生
- [ ] **W6 收尾** — Excel 匯出、Docker Compose、CI、完整文件

## 關於資料

本專案使用**合成資料**。作者手上沒有真實的平台結算單，
因此 `scripts/gen_fixtures.py` 依據公開的結算單欄位結構產生測試資料。

但合成不等於乾淨。產生器會刻意注入真實情境會出現的狀況，
每一種都對應對帳引擎必須處理的一個問題：

| 注入的狀況 | 對帳引擎要怎麼處理 |
|---|---|
| 訂單編號加前綴／大小寫不一 | 正規化後 Stage 1 仍可精確匹配 |
| 訂單編號整欄空白 | Stage 1 找不到，落到 Stage 3 模糊匹配 |
| 金額與我方預期不符 | Stage 2 容差匹配，計算差額並歸因 |
| 部分退款獨立成一列 | 同一訂單出現多列，不能假設一對一 |
| 平台重複匯出同一列 | 匯入時的冪等性要擋掉 |
| 平台有、我方無 | 漏記單 |
| 我方有、平台無 | 未撥款（跨月結算或真的沒收到） |

產生器同時輸出 `manifest.json` 作為 **ground truth**，記錄每一種狀況實際
注入了哪幾筆。W3 的 benchmark 會拿它來驗證「自動匹配率」這個數字是真的
算對了，而不是自己跟自己說好聽話。

所有資料由亂數種子決定，同一個種子永遠產生同一份資料，因此測試可重現。
這一點在此明確說明，不做任何有真實資料的暗示。
